export async function render() {
  document.getElementById('content').innerHTML =
    '<div class="card"><h2>Dashboard</h2><p>WIP skeleton — health summary and provider status will appear here.</p></div>';
}
